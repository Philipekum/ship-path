import numpy as np
a = np.zeros((180, 360), dtype=int)
b = np.ones_like(a, dtype=int)
# def coords_maker(i, j):
#     arr = np.zeros_like(a, dtype=int)
#
# for i in range(len(a)):
#     for j in range(len(a[i])):
#         print(a[i][j])
